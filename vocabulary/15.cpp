#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
double r;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> r;
    cout << fixed << setprecision(6);
    cout << M_PI * r * r << " " << 2 * M_PI * r << endl;
    return 0;
}
