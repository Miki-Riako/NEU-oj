#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
int a, b;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> a >> b;
    cout << a * b << " " << 2 * (a + b) << endl;
    return 0;
}
