#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
int number;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> number;
    cout << number * number * number << endl;
    return 0;
}
