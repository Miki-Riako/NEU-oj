#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    for (int i = 0; i < 1000; ++i)
        cout << "Hello World" << endl;
    return 0;
}
